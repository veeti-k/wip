{
	"name": "backend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "yarn build && nodemon -q -e ts,json --watch ../../packages --watch ./src --watch ../../.env --exec \"yarn build && yarn dotenv -e ../../.env -v PORT=5000 node --experimental-specifier-resolution=node ./dist/index.js\"",
		"build": "rm -rf ./dist && tsc",
		"start": "node --experimental-specifier-resolution=node ./dist/index.js"
	},
	"dependencies": {
		"@fastify/cors": "^8.2.0",
		"@gym/api": "*",
		"@trpc/server": "^10.4.1",
		"fastify": "^4.10.2"
	},
	"devDependencies": {
		"@gym/tsconfig": "*",
		"@types/node": "^18.11.9",
		"nodemon": "^2.0.20",
		"typescript": "^4.9.3"
	}
}
